[package]
name = "lumen-os"
version = "0.1.0"
edition = "2021"

[dependencies]
cortex-m-rt = "0.7"
cortex-m-semihosting = "0.5"
panic-halt = "0.2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
